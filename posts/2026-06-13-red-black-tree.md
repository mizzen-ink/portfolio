---
title: "数据结构：红黑树原理与实现"
date: 2026-06-13
tags: ["数据结构", "红黑树", "C/C++", "算法"]
description: "红黑树是一种自平衡二叉查找树，是map、set等关联式容器的核心底层数据结构。本文详细讲解其原理与实现。"
---

## 什么是红黑树？

红黑树是一种自平衡的二叉查找树，它在每个节点上增加了一个存储位表示节点的颜色（红或黑）。通过对任何一条从根到叶子的路径上各个节点着色方式的限制，红黑树确保最长路径不超过最短路径的两倍。

## 五大性质

1. **每个节点是红色或黑色**
2. **根节点是黑色**
3. **叶节点（NIL）是黑色**
4. **红色节点的两个子节点都是黑色**（不能有连续的红色节点）
5. **从任一节点到其每个叶子的所有路径都包含相同数量的黑色节点**

## 旋转操作

### 左旋

```cpp
void leftRotate(Node* x) {
    Node* y = x->right;
    x->right = y->left;
    if (y->left != NIL) y->left->parent = x;
    y->parent = x->parent;
    if (x->parent == NIL) root = y;
    else if (x == x->parent->left) x->parent->left = y;
    else x->parent->right = y;
    y->left = x;
    x->parent = y;
}
```

### 右旋

与左旋对称。

## 插入操作

插入新节点默认是**红色**。然后根据情况进行调整：

| 情况 | 操作 |
|-----|------|
| 父节点是黑色 | 直接插入，无需调整 |
| 父节点红色，叔叔节点红色 | 变色 + 向上递归 |
| 父节点红色，叔叔节点黑色 | 旋转 + 变色 |

## 应用场景

- **C++ STL map / set** 的底层实现
- **Linux 内核** 的虚拟内存管理
- **epoll** 中管理注册的文件描述符
- **Java HashMap**（冲突过多时转红黑树）
